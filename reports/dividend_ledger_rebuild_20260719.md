# 红利策略账本重建报告（2026-07-19）

## 结论

`dividend_a_stock` 的历史账本已经在独立数据库中完成确定性重建并通过放行检查。
旧账不需要因“存在旧记录”而整体隔离；真正需要作废的是能够被审计证据证明由复权混算
产生的伪交易事件。生产数据库截至本报告生成时保持只读未变，原因是当前时间不在已批准的
周六 14:00–16:00（Asia/Shanghai）生产维护窗口。

## 根因与证据

- BaoStock 与新浪在相关交易日给出的原始 OHLC 相互一致。
- 2026-07-12 11:08 的备份仍保存原始成交价格；11:31 的备份已经被后复权收盘价覆盖。
- 旧 `repair_db_prices.py` 把复权收盘价写入成交字段，后续又与原始实时价比较，进而触发
  虚假加仓、虚假止损和现金重复扣减。
- 每日研究结果、污染前备份、交易日历及双源原始行情共同确定了可恢复的真实事件序列；
  缺失的 `601965` 买卖能够完整补录。

## 重建结果（截至 2026-07-17）

| 项目 | 结果 |
| --- | ---: |
| 现金 | 932,716.73 CNY |
| 持仓市值 | 66,936.96 CNY |
| NAV | 999,653.69 CNY |
| 已实现 PnL | -1,283.27 CNY |
| 已平仓交易 | 18 笔 |
| 当前持仓 | `000719`、`600012` |

当前持仓的原始成交信息：

- `000719`：2026-07-09，11.93，1 份。
- `600012`：2026-07-14，16.87，1 份。

2026-07-17 新产生的十只研究候选没有倒填为历史成交，等待下一次合法交易时段处理。

## 实现与安全边界

- 声明式清单固定事件、双源 OHLC 边界、作废事件、每日 NAV 与最终状态。
- 重建工具只接受源库并写入一个不存在的新数据库路径，不原地修改源库。
- 源库以只读模式打开；重建前后比较源文件 SHA-256。
- 价格必须同时落在 BaoStock 与新浪原始 low/high 内。
- 拒绝重复买入、无持仓卖出、份数不匹配、负现金和 NAV 不闭合。
- `strategy_daily_results` 整表哈希必须不变；其他策略的账本表逐行摘要必须不变。
- 输出库迁移到 v6，固定为 `journal_mode=DELETE`，不依赖 WAL/SHM 边车文件。
- `repair_db_prices.py` 已改为纯禁用入口，历史危险实现不再可执行或导入。

## 验证结果

- 专项重建测试：4 passed。
- 账本、交易完整性、NAV、执行账本、writer fence、生产发布回归：55 passed。
- private Core 最新 `main` 全量 quant-strategy 测试：301 passed，1 deselected。
- SQLite `PRAGMA integrity_check`：`ok`。
- SQLite `PRAGMA foreign_key_check`：0 项。
- `check_db_integrity.py`：通过。
- `check_ledger_sanity.py --effective-date 2026-07-17`：通过。
- 重建重复运行输出数据库 SHA-256 一致。
- 当日新仓 NAV 接口已统一为显式未结算持仓集合：只有启用会话身份校验且成交日与
  活跃会话一致时按成本乘数 1.0 估值；会话不明、不匹配或未来日期均拒绝并回滚。

## 产物与哈希

- 源生产库 SHA-256（未变）：
  `eb3e442787f136d6cbae5161b43170dfd70eedf8514dbf9d3437d2fd3825c5c0`
- 事件清单 SHA-256：
  `e02c6edb16ffebc8e71c752ab878138a0524d54f80bee7865ecb16546fa64d68`
- 已验证重建库 SHA-256：
  `4ab57d33367922cb72935fb67cfac057c8b9941b73717b4da3daa688356aa870`

## 生产放行

生产更新应在下一个明确批准的周六 14:00–16:00（Asia/Shanghai）窗口执行。放行时需：

1. 停止所有 writer 并取得共享 writer fence。
2. 重新校验生产源哈希与 WAL 状态，生成即时 Online Backup。
3. 重新运行重建和全部只读检查，确认输出哈希与本报告一致。
4. 原子发布已验证数据库，随后再次检查完整性、账户、持仓、NAV 和源/目标哈希。
5. 保留发布前备份、重建报告和恢复说明；失败时不修改生产库。
