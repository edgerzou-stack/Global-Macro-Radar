# Security Policy

请勿在公开 issue、日志或报告中提交 API key、SMTP 授权码、数据库、账户信息或
未脱敏的运行 artifact。公开发行版不会包含生产数据库、量化策略、测试 fixture 或
内部操作说明。

发现疑似凭证泄露时，应立即轮换凭证；删除 Git 当前版本不能撤回已经进入历史或被
第三方获取的内容。

发布候选必须由仓库提供的 copy-only exporter 生成，并同时通过精确路径、符号链接、
已知密钥格式、文件哈希和 Python 语法检查。不要通过扩大目录白名单绕过失败。
